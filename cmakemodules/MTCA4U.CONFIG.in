# A configuration to be loaded in regular Makefile
#
# It provides variables to link against the libraries provided
# by this MTCA4U installation. See ReadMe.DOOCS.txt for more information.

# Note: For custom installations where not all sub-packages are installed the
# variables for the missing packages are set, but are invalid and must not be used.
# As the package is missing, the compilation will fail anyways if the missing
# sub-package was required.

MtcaMappedDevice_INCLUDE_FLAGS=-I@MtcaMappedDevice_DIR@/include
MtcaMappedDevice_LIB_FLAGS=-L@MtcaMappedDevice_DIR@/lib -lMtcaMappedDevice
MtcaMappedDevice_RUNPATH_FLAGS=-Wl,-rpath=@MtcaMappedDevice_DIR@/lib,--enable-new-dtags

MotorDriverCard_INCLUDE_FLAGS= $(MtcaMappedDevice_INCLUDE_FLAGS) -I@MotorDriverCard_DIR@/include
MotorDriverCard_LIB_FLAGS= $(MtcaMappedDevice_LIB_FLAGS) -L@MotorDriverCard_DIR@/lib -lMotorDriverCard
MotorDriverCard_RUNPATH_FLAGS= $(MtcaMappedDevice_RUNPATH_FLAGS) -Wl,-rpath=@MotorDriverCard_DIR@/lib,--enable-new-dtags

MTCA4U_DIR=@MTCA4U_DIR@
